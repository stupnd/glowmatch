# Runtime dependencies for the API server.
#
# torch/torchvision are deliberately NOT here. Nothing in the request path
# imports them — see requirements-train.txt. Installing them added ~543MB to
# the deploy for code that never ran.

fastapi>=0.115.0
httpx>=0.27.0
ddgs>=0.1.0
anthropic>=0.20.0
python-dotenv>=1.0.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
Pillow>=10.0.0
opencv-python>=4.10.0
mediapipe>=0.10.0
